services:
    app:
        image: thisisduykhanh/sope-data-service:latest
        container_name: image_service
        ports:
            - "${APP_PORT}:${APP_PORT}"
        env_file:
            - .env
        volumes:
            - ./models:/app/models
        restart: unless-stopped
